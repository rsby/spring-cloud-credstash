credstash:
  enabled: true
  table: "test-store"
  addPrefix: "dev_"
  matching: "**.secret"
  properties:
    pass:
      matching: "**.pass"
    key:
      stripPrefix: "credstash__"
      matching: "credstash__*"
      context:
        my-app: "key"
