test:
  secret: "not from credstash"
  test: "test"
  pass: "not from credstash"
  key: ${credstash__test_path_key}

credstash__test.path.key:
my.exact.match: "not from credstash"

try.override: not overridden

