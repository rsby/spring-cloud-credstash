credstash:
  enabled: true
  table: "test-store"
  addPrefix: "dev_"
  matching: "**.secret"
  more:
    pass:
      matching: "**.pass"
    key:
      stripPrefix: "credstash__"
      matching: "credstash__*"
      context:
        my-app: "key"
