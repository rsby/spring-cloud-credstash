credstash:
  enabled: true
  table: "test-store"
  addPrefix: "dev_"
  matching:
   - "**.secret"
   - "my.exact.match"
   - "test.from*"
  more:
    pass:
      matching: "**.pass"
    key:
      stripPrefix: "credstash__"
      matching: "credstash__*"
      context:
        my-app: "key"
    test:
      enabled: false
      matching: "test.test"
